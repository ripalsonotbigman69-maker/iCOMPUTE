# This backend uses SQLite only.
# No MySQL environment variables are required.

# SMTP settings for password reset email delivery.
EMAIL_SMTP_HOST=smtp.example.com
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USER=your-smtp-username
EMAIL_SMTP_PASSWORD=your-smtp-password
EMAIL_FROM=icompute@example.com
